server:
  port: 8088
  
spring:
  application:
    name: CONFIG-SERVER  
  
  cloud:
    config:
      server:
        git:
          uri: https://github.com/subhabratabanerjee716/microservice-config
          
          clone-on-start: true
          